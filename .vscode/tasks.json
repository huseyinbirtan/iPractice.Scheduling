{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dotnet: build iPracticeApi",
            "type": "dotnet",
            "task": "build",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}\\iPractice.Schedule.Api"
            }
        }
    ]
}